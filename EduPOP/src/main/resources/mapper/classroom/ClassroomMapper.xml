<?xml version="1.0" encoding="UTF-8"?>
<!-- XML 선언: UTF-8 인코딩을 명시합니다. -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- MyBatis DTD 선언: 이 파일이 MyBatis 매퍼 규격을 따른다는 정의문입니다. -->

<!-- namespace 속성에는 이 XML과 짝을 이루는 실제 Java Mapper 인터페이스의 풀 패키지 경로를 작성합니다. -->
<mapper namespace="com.example.EduPOP.repository.classroom.ClassroomMapper">

    <!-- 반 기본 정보 등록 (생성된 AUTO_INCREMENT PK는 classId 필드로 주입) -->
    <insert id="insertClass" parameterType="classroom"
            useGeneratedKeys="true" keyProperty="classId">
        INSERT INTO classes(academy_id,
                            name,
                            target_grade,
                            max_students,
                            status,
                            description)
                    VALUES(#{academyId},
                            #{name},
                            #{targetGrade},
                            #{maxStudents},
                            #{status},
                            #{description})
    </insert>

    <!-- 반-강사 매핑 등록 (다중 강사 N:M 배정) -->
    <insert id="insertClassTeacher" parameterType="classTeacher">
        INSERT INTO class_teachers (class_id, teacher_id, role_type, created_at)
        VALUES (#{classId}, #{teacherId}, #{roleType}, NOW())
    </insert>

    <!-- 반-강사 다중 배정 매핑 벌크 등록 (벌크 인서트 최적화) -->
    <insert id="insertClassTeachers" parameterType="list">
        INSERT INTO class_teachers (class_id, teacher_id, role_type, created_at)
        VALUES
        <foreach collection="teachers" item="item" separator=",">
            (#{item.classId}, #{item.teacherId}, #{item.roleType}, #{item.createdAt})
        </foreach>
    </insert>

    <!-- 동일 학원 내 활성 상태(ACTIVE) 반 이름 중복 검사 -->
    <select id="findByNameAndAcademyId" resultType="classroom">
        SELECT * FROM classes WHERE name = #{name} AND academy_id = #{academyId} AND status = 'ACTIVE'
    </select>

    <!-- 반 상태 변경(ACTIVE / CLOSED) -->

    <update id="updateStatus">
        UPDATE classes
        SET status = #{status},
            updated_at = NOW()
        WHERE class_id = #{classId}
    </update>

    <!-- 다중 일괄 상태 변경 (IN 절 동적 생성) -->
    <update id="updateStatusesBulk">
        UPDATE classes
        SET status = #{status},
        updated_at = NOW()
        WHERE class_id IN
        <foreach collection="classIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 등록 된 반 리스트 가져오기 -->
    <select id="findAllByAcademyId" resultType="com.example.EduPOP.controller.classroom.dto.ClassroomListResponse">
        SELECT
        c.class_id AS classId,
        c.name AS name,
        c.target_grade AS targetGrade,
        c.max_students AS maxStudents,
        c.status AS status,
        c.created_at AS createdAt,
        u.name AS mainTeacherName
        FROM classes c
        LEFT JOIN class_teachers ct ON c.class_id = ct.class_id AND ct.role_type = 'MAIN'
        LEFT JOIN users u ON ct.teacher_id = u.user_id
        WHERE c.academy_id = #{academyId}
        <if test="status != null and status != '' and status != 'ALL'">
            AND c.status = #{status}
        </if>
        ORDER BY c.class_id DESC
    </select>

    <!-- 반 상세 기본 정보 조회 -->
    <select id="findById" resultType="com.example.EduPOP.controller.classroom.dto.ClassroomDetailResponse">
        SELECT
            class_id AS classId,
            academy_id AS academyId,
            name AS name,
            target_grade AS targetGrade,
            max_students AS maxStudents,
            status AS status,
            description AS description,
            created_at AS createdAt
        FROM classes
        WHERE class_id = #{classId}
    </select>

    <!-- 반에 배정된 강사 목록 조회 -->
    <select id="findTeachersByClassId" resultType="com.example.EduPOP.controller.classroom.dto.ClassroomDetailResponse$TeacherInfo">
        SELECT
            ct.teacher_id AS teacherId,
            u.name AS name,
            ct.role_type AS roleType
        FROM class_teachers ct
                 JOIN users u ON ct.teacher_id = u.user_id
        WHERE ct.class_id = #{classId}
        ORDER BY CASE WHEN ct.role_type = 'MAIN' THEN 1 ELSE 2 END
    </select>

    <!-- 반 기본 정보 수정 -->
    <update id="updateClass">
        UPDATE classes
        SET name = #{name},
            target_grade = #{targetGrade},
            max_students = #{maxStudents},
            status = #{status},
            description = #{description},
            updated_at = NOW()
        WHERE class_id = #{classId}
    </update>

    <!-- 강사 중복 배정 여부 확인 -->
    <select id="countClassTeacher" resultType="int">
        SELECT COUNT(*)
        FROM class_teachers
        WHERE class_id = #{classId} AND teacher_id = #{teacherId}
    </select>

    <!-- 강사 매핑 삭제 -->
    <delete id="deleteClassTeacher">
        DELETE FROM class_teachers
        WHERE class_id = #{classId} AND teacher_id = #{teacherId}
    </delete>

    <!-- 학원 소속 강사 목록 조회 -->
    <select id="findTeachersByAcademyId" resultType="com.example.EduPOP.controller.classroom.dto.ClassroomDetailResponse$TeacherInfo">
        SELECT
            user_id AS teacherId,
            name AS name
        FROM users
        WHERE academy_id = #{academyId}
          AND role = 'TEACHER'
        ORDER BY name ASC
    </select>

    <!-- 현재 반의 수강생 수 조회 -->
    <select id="countStudentsByClassId" resultType="int">
        SELECT COUNT(*) FROM class_students WHERE class_id = #{classId}
    </select>

    <!-- 특정 학생의 반 중복 배정 여부 확인 -->
    <select id="existsClassStudent" resultType="int">
        SELECT COUNT(*) FROM class_students WHERE class_id = #{classId} AND student_id = #{studentId}
    </select>

    <!-- 반 수강생 매핑 등록 -->
    <insert id="insertClassStudent">
        INSERT INTO class_students (class_id, student_id, enrolled_at)
        VALUES (#{classId}, #{studentId}, NOW())
    </insert>

    <!-- 반 수강생 매핑 삭제(퇴원) -->
    <delete id="deleteClassStudent">
        DELETE FROM class_students
        WHERE class_id = #{classId} AND student_id = #{studentId}
    </delete>

    <!-- 학원 소속 전체 학생 풀 조회 (학년 및 전화번호 포함) -->
    <select id="findStudentPoolByAcademyId" resultType="com.example.EduPOP.controller.classroom.dto.ClassroomDetailResponse$StudentInfo">
        SELECT
        u.user_id AS studentId,
        u.name AS name,
        u.phone AS phone,
        u.school_grade AS grade,
        COALESCE(MAX(c.name), 'NONE') AS currentClassName
        FROM users u
        LEFT JOIN class_students cs ON u.user_id = cs.student_id
        LEFT JOIN classes c ON cs.class_id = c.class_id AND c.status = 'ACTIVE'
        WHERE u.academy_id = #{academyId}
        AND u.role = 'STUDENT'
        <!-- 현재 반에 이미 배정된 학생은 대기 풀(왼쪽)에서 제외 -->
        AND u.user_id NOT IN (
        SELECT COALESCE(student_id, 0)
        FROM class_students
        WHERE class_id = #{classId}
        )
        GROUP BY u.user_id, u.name, u.phone, u.school_grade
        ORDER BY u.name ASC, u.user_id ASC
    </select>

    <!-- 현재 반의 기존 수강생 전체 비우기 -->
    <delete id="deleteAllStudentsByClassId">
        DELETE FROM class_students
        WHERE class_id = #{classId}
    </delete>

    <!-- 전반 대상 학생들의 기존 소속 반 매핑 일괄 삭제 -->
    <delete id="deleteOtherClassMapping">
        <if test="studentIds != null and studentIds.size() > 0">
            DELETE FROM class_students
            WHERE student_id IN
            <foreach collection="studentIds" item="studentId" open="(" separator="," close=")">
                #{studentId}
            </foreach>
        </if>
    </delete>

    <!-- 새로운 수강생 목록 일괄 삽입 -->
    <insert id="batchInsertStudents">
        INSERT INTO class_students (class_id, student_id, enrolled_at)
        VALUES
        <foreach collection="studentIds" item="studentId" separator=",">
            (#{classId}, #{studentId}, NOW())
        </foreach>
    </insert>

    <!-- 특정 클래스에 속한 학생 목록 조회 -->
    <select id="findStudentsByClassId" resultType="com.example.EduPOP.controller.classroom.dto.ClassroomDetailResponse$StudentInfo">
        SELECT
            u.user_id AS studentId,
            u.name AS name,
            u.email AS email,
            u.phone AS phone,
            c.name AS className
        FROM class_students cs
                 JOIN users u ON u.user_id = cs.student_id
                 JOIN classes c ON c.class_id = cs.class_id
        WHERE cs.class_id = #{classId}
          AND cs.status = 'ACTIVE'
    </select>

    <!-- 학생 기본 정보 수정 쿼리 -->
    <update id="updateStudentInfo">
        UPDATE users
        SET
            name = #{name},
            email = #{email},
            phone = #{phone},
            updated_at = NOW()
        WHERE user_id = #{studentId}
    </update>


</mapper>