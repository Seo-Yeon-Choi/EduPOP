<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.EduPOP.repository.classroom.ClassMapper">

    <select id="findByTeacherId"
            resultType="com.example.EduPOP.domain.user.AcademyClass">

        SELECT
        c.class_id,
        c.academy_id,
        c.name,
        c.target_grade,
        c.max_students,
        c.status,
        c.description,
        c.created_at,
        c.updated_at

        FROM classes c

        INNER JOIN class_teachers ct
        ON c.class_id = ct.class_id

        WHERE ct.teacher_id = #{teacherId}
        AND c.status = 'ACTIVE'

        ORDER BY c.name

    </select>

</mapper>