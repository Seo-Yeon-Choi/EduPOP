<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.EduPOP.repository.user.AcademyMapper">

    <insert id="save" parameterType="com.example.EduPOP.domain.user.Academy" useGeneratedKeys="true" keyProperty="academy_id">
        INSERT INTO academies (name, address, phone, business_cer, created_at, updated_at)
        VALUES (#{name},#{address},#{phone},#{business_cer},NOW(),NOW())
    </insert>

</mapper>
